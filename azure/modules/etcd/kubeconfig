apiVersion: v1
clusters:
- cluster:
    ${ cluster }
  name: local
contexts:
- context:
    cluster: local
    user: ${ user }
  name: ${ name }
current-context: ${ name }
kind: Config
users:
- name: ${ user }
  user:
    ${ user_authentication }

