# datacenter is the name of the datacenter to which the folder belongs
variable "datacenter" {
  type = "string"
}

# folder_path is the path to the folder in which the VMs are created
variable "folder_path" {
  type = "string"
}
