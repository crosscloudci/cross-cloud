apiVersion: v1
kind: Config
users:
- name: ${ user }
  user:
    ${ user_authentication }
clusters:
- name: local
  cluster:
    ${ cluster }
contexts:
- context:
    cluster: local
    user: ${ user }
  name: ${ name }
current-context: ${ name }
